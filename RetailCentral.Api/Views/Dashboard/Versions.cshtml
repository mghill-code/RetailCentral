@model List<RetailCentral.Api.ViewModels.VersionSummaryViewModel>

<h2>Agent Version Summary</h2>

<table>
    <thead>
        <tr>
            <th>Agent Version</th>
            <th>Total Devices</th>
            <th>Online</th>
            <th>Offline</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var v in Model)
        {
            <tr>
                <td>@v.AgentVersion</td>
                <td>@v.DeviceCount</td>
                <td class="online">@v.OnlineCount</td>
                <td class="offline">@v.OfflineCount</td>
            </tr>
        }
    </tbody>
</table>